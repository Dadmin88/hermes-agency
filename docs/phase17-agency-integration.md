# Phase 17 Hermes Agency integration

This branch synchronizes the reviewed Hermes Keryx Python SDK revision into `src/keryx` and verifies the existing Agency lifecycle, incoming handler, durable task handle, artifact reconciliation, and Kanban pending-review seams against the new result protocol.

The synchronized revision is recorded in `src/keryx/_sync_manifest.json` so future updates remain deterministic and reviewable.
