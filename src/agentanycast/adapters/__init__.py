"""Framework adapters for AgentAnycast.

Each adapter wraps an existing AI framework (CrewAI, LangGraph, Google ADK,
OpenAI Agents SDK) and exposes it as a P2P A2A agent with zero changes to
the framework's business logic.
"""
